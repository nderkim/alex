{
  "name": "alex",
  "version": "0.0.0",
  "bin": "./bin.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "pack": "node scripts/pack"
  },
  "devDependencies": {
    "pkg": "^4.4.9"
  }
}
